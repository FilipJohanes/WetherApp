// Removed old authentication system - now using Flask sessions
// All authentication is handled server-side via Flask login system

document.addEventListener('DOMContentLoaded', function() {
  console.log('Daily Brief - Using Flask authentication');
});
